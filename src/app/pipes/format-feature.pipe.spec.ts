import { FormatFeaturePipe } from './format-feature.pipe';

describe('FormatFeaturePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatFeaturePipe();
    expect(pipe).toBeTruthy();
  });
});
